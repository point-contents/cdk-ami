name: InstallAngular
description: this document installs node on ubuntu
schemaVersion: 1.0

phases:
    - name: build
      steps:
        - name: InstallTmux
          action: ExecuteBash
          inputs:
            commands:
                - sudo yum update -y
                - sudo yum install tmux -y
